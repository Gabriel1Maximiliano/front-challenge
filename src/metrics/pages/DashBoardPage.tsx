import { Layout } from "../layouts";
import { Typography } from "@mui/material";

export const DashBoardPage = () => {
  return (
    <Layout>
      <Typography variant="h1">Soy DashBoard</Typography>
    </Layout>
  );
};
